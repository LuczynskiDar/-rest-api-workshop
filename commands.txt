# History file, http://woshub.com/powershell-commands-history/
%userprofile%\AppData\Roaming\Microsoft\Windows\PowerShell\PSReadline\ConsoleHost_history.txt
dotnet new webapi -n Songify.Minimal -o ./Songify.Minimal
dotnet sln add .\Songify.Minimal\*.csproj
dotnet sln add (ls -r **/*.csproj)
dotnet tool install --global dotnet-ef

docker-compose up -d
$ dotent ef migrations add InitialMigration -v
$ dotnet ef database update -v

Open API, definicja API w Json, dla IBM API connec5
RFC7807

#lista temlate'ow tool-manifest
dotnet new
# sprawdzic build co sie nazywa cake

# fluentmigrator i dbup do rozdzielenia migracji od startu aplikacji